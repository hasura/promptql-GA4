kind: DataConnector
version: v1
definition:
  name: ga4
  title: Google Analytics 4 Connector for PromptQL
  description: GA4 integration for PromptQL natural language analytics
  configuration:
  properties:
    property_id:
      type: string
      description: GA4 Property ID (e.g., 123456789)
    credentials:
      type: string
      description: Service account JSON credentials
      format: password
